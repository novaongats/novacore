# Cloudflare Pages カスタムヘッダー
#
# /v2/ はビルドなし ES Modules のため、モジュールの版ズレ
# （新旧が混在してアプリが壊れる）を防ぐには常時再検証が必須。
# no-cache は「使う前に必ずサーバーへ確認」（ETag 304 なら転送なし）で、
# 表示速度への影響は最小のまま、デプロイ即反映になる。

/v2/*
  Cache-Control: no-cache
